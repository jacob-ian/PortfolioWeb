import { EducationException } from './education-exception';

describe('EducationException', () => {
  it('should create an instance', () => {
    expect(new EducationException()).toBeTruthy();
  });
});
